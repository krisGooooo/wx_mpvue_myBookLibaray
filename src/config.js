//  配置项

// const host = 'http://localhost:5757'
const host = 'https://gmt1keva.qcloud.la'

// https://gmt1keva.qcloud.la

const config = {
  host,
  loginUrl: `${host}/weapp/login`
}

export default config
